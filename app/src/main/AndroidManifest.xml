<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ALL4OCEAN"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ALL4OCEAN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true"/>

        <activity
            android:name=".CadastroActivity"
            android:exported="true"
            />

        <activity
            android:name=".ResetarSenhaActivity"
            android:exported="true"/>

        <activity
            android:name=".UsuarioLogado"
            android:exported="true"/>

        <activity
            android:name=".AtualizarCadastro"
            android:exported="true"/>

        <activity
            android:name=".ComoAjudarActivity"
            android:exported="true"/>

        <activity
            android:name=".ListaCuriosidadesActivity"
            android:exported="true"/>

        <activity
            android:name=".SobreActivity"
            android:exported="true"/>

        <activity
            android:name=".DetalheCuriosidadeActivity"
            android:exported="true"/>



    </application>

</manifest>